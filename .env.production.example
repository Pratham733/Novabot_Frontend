# Frontend production environment example
EXPO_PUBLIC_API_BASE=https://api.example.com
# Set to /api/v1 once backend version promoted
a# (leave blank or /api for legacy)
EXPO_PUBLIC_API_VERSION=/api
# When ready to move to versioned routes:
# EXPO_PUBLIC_API_VERSION=/api/v1

# Optional telemetry
# EXPO_PUBLIC_SENTRY_DSN=https://xxxx.ingest.sentry.io/12345

# Feature flags (override if disabling)
# EXPO_PUBLIC_ENABLE_FIREBASE_AUTH=true
